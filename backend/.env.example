# Copy this file to `.env` in the same folder and fill in your key.
# `.env` is gitignored - never commit a real key.
# Do NOT put your real key in THIS file: .env.example IS committed.
ANTHROPIC_API_KEY=sk-ant-...
